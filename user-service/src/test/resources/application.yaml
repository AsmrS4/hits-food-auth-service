server:
  port: 8910
  error:
    whitelabel:
      enabled: false

spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:test_db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: sa

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
      flyway:
        enabled: true
        locations: classpath:/db/test
        baseline-on-migrate: true
    show-sql: true

jwt:
  secret: hits2025972303hits2025972303hits20259723032025hits
  lifetime: 30m
  refresh-lifetime: 14400m

url:
  order-service-url: http://localhost:8096

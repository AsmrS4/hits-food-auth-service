server:
  port: 8000

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true

    compatibility-verifier:
      enabled: false

jwt:
  secret: hits2025972303hits2025972303hits20259723032025hits
  lifetime: 30m
